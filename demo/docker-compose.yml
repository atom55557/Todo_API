version: '3.8'
services:
  db:
    image: postgres:15
    container_name: todo-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: tododb